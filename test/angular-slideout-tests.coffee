chai = require 'chai'
sinon = require 'sinon'

{Angularslideout} = require '../src/angular-slideout'

chai.should()

describe 'angular-slideout', ->
  it 'should do something', ->
    true.should.equal true